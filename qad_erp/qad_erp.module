<?php

/**
 * @file
 * Primary module hooks for qad_erp module.
 */

/**
 * Implements hook_theme().
 */
function qad_erp_theme($existing, $type, $theme, $path) {
  return [
    'erp_block' => [
      'variables' => ['items' => NULL],
    ],
    
  ];
}